{
  "name": "introspection-forms-monorepo",
  "private": true,
  "packageManager": "yarn@4.16.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -A run build",
    "dev": "yarn workspaces foreach -A run dev",
    "typecheck": "yarn workspaces foreach -A run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
